# Backblaze B2 S3-compatible configuration
# Region example: us-west-000 or eu-central-003
B2_REGION=us-west-000
# The target bucket name (must exist and be Public for direct links)
B2_BUCKET=your-bucket-name
# S3 endpoint for Backblaze (usually https://s3.<region>.backblazeb2.com)
B2_ENDPOINT=https://s3.us-west-000.backblazeb2.com
# Application Key ID
B2_KEY_ID=your-key-id
# Application Key
B2_APPLICATION_KEY=your-application-key
# Force path-style URLs for compatibility. Keep true unless you set a custom domain
B2_FORCE_PATH_STYLE=true
# Optional: if using a custom domain or CDN, set base public URL
# e.g., https://cdn.example.com or https://f004.backblazeb2.com/file/your-bucket
# If unset, will default to https://<bucket>.s3.<region>.backblazeb2.com
B2_PUBLIC_URL_BASE=